var S = S || {}; // If S (Stopwatch) is not defined, create new object
S = (function () {
	
    // Initialize the application
    var init = function () {
        
    };
     
    // Return the public facing methods for the App
    return {
        init: init
    };
}());
 
S.init();